#pragma once

// Repertório de humor do robô — inspirado no jeito "vivo" dos personagens
// Pixar: nunca um estado só, sempre uma pequena variação de olhar/postura.
enum class Mood {
    Idle,
    Curious,
    Sleepy,
    Happy,
    Scared,
    Smiling,
    LookLeft,
    LookRight,
    LookUp,
    LookUser,
};
